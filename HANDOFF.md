# Handoff — Kalinga Sthapatya (updated 2026-09-25)

New session: read this, then `CLAUDE.md` → `BUILD_STATUS.md`. Everything durable is on disk and in git.
**Token budget is limited — work lean: Sonnet for builders/integrator, Opus for research/fact-check, one agent per
job, short reports. Big design questions go to an expert (Plan) agent.** The human wants research, fact-check and
build tasks delegated to Sonnet/Opus subagents; the main session reviews, merges, verifies and commits.

## 1. Objective

Static educational site on Odisha (Kalinga) temple architecture for families/kids, on GitHub Pages
(repo https://github.com/sabyasachi-swain/kalinga-sthapatya → https://sabyasachi-swain.github.io/kalinga-sthapatya/).
Every fact cited (academic/ASI/UNESCO only), unverifiable → blank; facts live only in `data/*.json`; pages are
pre-rendered by `node scripts/build.mjs`; all visuals are generated by the human (Gemini/DALL·E).

## 2. Standing decisions (don't re-ask)

- **G2 publishing rule:** publish fact-checked established / scholarly / uncertain claims. Never publish needs-human,
  snippet/catalogue-only, rejected or sensitive claims (leave null).
- **Story voice** (also in auto-memory): claim `text` = kid-friendly story (ages ~8–12), vivid and simple; NO scholar
  names or debate in the main text — names and disagreements go in the collapsed "For curious grown-ups" note, and
  citations render as [1]. Prefer safe common ground ("more than 1,100 years old").
- **Tiers stay but quiet:** a small coloured dot + [n] inline; full pills only in the Sources legend.
- **Annotated images are allowed for Parasuramesvara** (human override of the "no text in images" rule) — but every
  label must be word-for-word from fact-checked data.
- **Eras:** six descriptive labels (Rock-cut roots · First temples · Experiment & refinement · Monumental scale ·
  Grand finale · Late Kalinga). The ids formative/transitional/mature are internal keys, never shown.
- **Images:** approve a clean PASS; keep JPEG; anything failing stays a placeholder.
- **Outside models write code and research:** the human runs the prompts in `CODING-PROMPTS*.md` /
  `research/RESEARCH-PROMPTS.md` through Gemini, then Claude reviews everything before it is committed.
- **Push:** the human approves each push explicitly.

## 3. State now

| Area | State |
|---|---|
| Data | 5 temples (konark, lingaraj, mukteshwar, parasuramesvara, jagannath-puri), 6 eras, 11 element **names only** (definitions null), 25 sources. `validate-content` PASS (11 warnings = registered but uncited sources) |
| Parasuramesvara | Story-voice text merged; page rebuilt as the 4-stop **journey** (Meet → How it was built → The parts → Its family) per `qa/TEMPLE-JOURNEY-SPEC.md`. Other 4 temples use the same template but still have research-style wording |
| Pages | 13 generated, including **real map.html and academy.html** (academy shows "Still being written" — `data/academy.json` is empty; that is correct) |
| Tooling | `node scripts/check-links.mjs` · `node scripts/qa-probe.mjs --port <free port>` (now 375/768/1024/1440 by default; inline citation links are reported separately as WCAG 2.5.8-exempt) · `node scripts/test-build.mjs` (13 build/snapshot tests; `--update` after an intentional markup change) |
| Coding round 2 | **C10–C18 all delivered.** C11 (tap-to-enlarge) was dead code — fixed in `f52cac6` (wired only on pages with `data-zoom` figures; Escape no longer leaves the page scroll-locked). UI/a11y audit `qa/UI-REVIEW-2026-09-23.md` fixed in `7db2229`; a site-wide type scale (`--text-*` tokens) replaced 29 ad-hoc font sizes |
| Images | 35 approved + **interim** V-64A/B/C (labels baked in, unsourced) + V-64V video (10 s, 9.5 MB, never preloaded). Rejected and awaiting regeneration: V-71, V-72, V-73, V-76, V-77, V-81, V-82 (reasons in `visual-briefs/BATCH-3-REVIEW.md`) |
| Research | `research/ledgers/parasuramesvara-2.json` (35 claims) — **fact-check running** (see §4). `research/leads/*.md` — refreshed 2026-09-25 by the human: T1–T5, A1–A5, C1 (+ a new `eras-dynasties.md`); the six old lead files were wrong and deleted on purpose — do NOT restore them. Leads only, never citable: they contain Wikipedia/Wikimedia/blogspot citations and unquoted inference (esp. `how-it-was-built.md`). Leads are uncommitted while the human is still adding files |
| Git | `origin/main` = 7db2229. Local `f52cac6` (C11 fix) NOT pushed — push only with the human's OK |
| GitHub Pages | **Live** at https://sabyasachi-swain.github.io/kalinga-sthapatya/ (verified 2026-09-25) |

## 4. Next steps, in order

**In flight (background subagents, launched 2026-09-25):**
- `kalinga-fact-checker` (Opus) on `parasuramesvara-2` → `research/verdicts/parasuramesvara-2.*`,
  `research/staged/parasuramesvara-2.json` (temples) and possibly `parasuramesvara-2-elements.json` (elements).
- `kalinga-researcher` (Opus) on A1+A2 → `research/ledgers/anatomy-terms.json` (glossary + element definitions).

1. **Review and merge the parasuramesvara-2 fragments.** `merge-staged.mjs` REPLACES whole objects by id — check
   the temples fragment still carries every existing parasuramesvara field/claim (diff against `data/temples.json`)
   before `--dry-run`, then merge → rebuild → full check suite. If the fragment relies on IGNCA or any other
   unregistered source, hold the merge until the human approves registering it (§5).
2. **Finalise `visual-briefs/PARASURAMESVARA-ANNOTATED-BRIEFS.md` (V-64A–F)** from the verdicts: drop the DRAFT
   banner; V-64C (construction) may need withdrawing (the ramp method is general Bhubaneswar, not this temple);
   V-64B's "13 m (44 ft)" label follows the height verdict. If labels change, the interim V-64A/B/C images carry
   wrong text and go back to placeholders. The human is waiting on this.
3. **Fact-check `anatomy-terms`** when the researcher returns (Opus fact-checker) → glossary terms + element
   `what`/`why`. Only these element ids exist: bada, bhogamandapa, gandi, horses, jagamohana, natamandira, pabhaga,
   pista, torana, vimana-base, wheel. Expect gaps: the A1 lead found no accepted source defining the pabhaga
   mouldings, jangha, baranda, raha, kanika.
4. **Story-voice rewrite: 4 temples + the eras** (one fact-checker run each; pattern in
   `research/verdicts/parasuramesvara-story.md`). The eras are new to this list: `data/timeline.json` still puts
   "(Brown); earlier per Panigrahi" and "Percy Brown's 'Middle Period'…" into kid-facing text on home, timeline
   and compare. Also clears "Never use for size comparisons." from temple notes.
5. **Later, by value:** C1 sizes (Lingaraja height is null), A4 construction (a Panigrahi lead ties the earth ramp
   to Lingaraja; Boner/Das *Baya Chakada* for Konark), T2–T5 new claims, then A3, A5. Each is a researcher +
   fact-checker pair (two Opus runs).
6. **Push** when the human approves.

## 5. Waiting on the human

- **Source registration:** IGNCA (cited by 5 leads and by parasuramesvara-2 claim 033) — recommended yes;
  *Odisha Review* (3 leads; state-government magazine, not an archaeology publication) — recommended per article.
- Committing the refreshed `research/leads/` once the human has finished adding files.
- Generate V-64A–F once step 2 says the briefs are final → drop into `supporting-artifacts/parsurameswar-temple/`.
- Regenerate the rejected images (V-71–73, V-76, V-77, V-81, V-82) and the older failures (V-32, V-33, V-35, V-37,
  V-42–45, V-83, V-84).
- Decide on Jagannath claims 022/023 (Buddhist/Jain/"primitive cult" origins) — sensitive, currently unpublished.
- Jagannath `temple_type` is "rekha" only because the validator requires the field (Ganguly calls it a pancharatha
  deul) — review when the architecture research lands.

## 6. Key knowledge (don't re-learn)

- **Commands:** build `node scripts/build.mjs` · staleness `--check` · content `node scripts/validate-content.mjs`
  (`--strict` before launch, `--staged <file>` for fragments) · merge `node scripts/merge-staged.mjs <file>` ·
  links `node scripts/check-links.mjs` · layout `node scripts/qa-probe.mjs --port 8097 --pages a.html,b.html`.
- **qa-probe quirks:** it fails with EADDRINUSE if a previous run left the port bound — pass a fresh `--port`, or
  kill the listener first (`Get-NetTCPConnection -LocalPort <p> -State Listen | Stop-Process -Id $_.OwningProcess`).
  Categories A/B/F/G are blockers; D (text < 14px) and E (targets < 44px) still report known shared-component
  issues that task C12 is meant to fix.
- **Screenshots:** serve the repo root (`python -m http.server 8080 --bind 127.0.0.1`), then
  `"C:/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --hide-scrollbars --screenshot=… --window-size=1440,900 <url>`.
  **Do not take very tall screenshots of temple pages:** tour steps are sized in `vh`, so a 7000px-tall window
  stretches the page and hides content. Instead use a 1440×900 (or 375×812 iframe) wrapper that scrolls to an
  offset, e.g. `<iframe src="…" style="width:1440px;height:900px">` plus `contentWindow.scrollTo(0, y)`.
  DOM probes: `--dump-dom --virtual-time-budget=4000` with a script writing results into a `<pre>`.
- **Prompt files:** `CODING-PROMPTS.md` (Block C = the shared rules; C1–C9, all delivered) ·
  `CODING-PROMPTS-2.md` (C10–C18) · `research/RESEARCH-PROMPTS.md` (Block G + 17 research topics).
  Every task prompt is written as Objective · Facts you may rely on · Steps · Guardrails · Output format · Acceptance,
  and tells the model to state assumptions rather than invent facts.
- **Recurring bugs in outside-model code** (check for these first when reviewing): treating `ctx.data.elements` or
  `ctx.data.media` as arrays (they are `{ elements: [] }` / `{ assets: [] }`); passing a whole `<script>` tag into
  `page()`'s `scripts` array (it takes paths); inlining the same SVG more than once without namespacing its ids;
  emitting class names that no CSS file styles; `<p>` nested inside `<p>` from wrapping `claim()` output;
  **a JS/CSS module written but never loaded by any page** (C11 shipped this way — grep `page()` calls for it);
  **accessibility overrides that select every `a` inside a card** (they hit inline citations and inflated them to
  44×44, breaking every `[1,2]` marker); `!important` used to paper over a wrong base rule; an `<img>` left in the
  DOM with no `src` (qa-probe G).
- **Typography contract:** every `font-size` comes from the `--text-*` scale in `css/style.css` `:root` — never a raw
  rem/px value (root is 16px, body 18px, floor 14px). Only Playfair 600/700 and Inter 400/600 are loaded; any
  other weight is faux-bolded. Card/panel titles are `--text-xl`. Inline citations are exempt from the 44px
  target rule (WCAG 2.5.8) and must stay inline.
- **Real-keyboard testing:** `dev/preview/cdp-keyboard-test.mjs` (git-ignored) drives headless Chrome over the
  DevTools protocol with real Tab/Enter/Escape (synthetic events don't trigger native `<dialog>` Escape). Node 20
  has no built-in WebSocket, so it carries a minimal client; wait for the 101 before sending. Run: start
  `python -m http.server 8124`, start Chrome with `--headless=new --remote-debugging-port=9335
  --user-data-dir=<tmp>`, then `node dev/preview/cdp-keyboard-test.mjs 9335 <url>`. It currently tests zoom.
- **Design contract:** `qa/TEMPLE-JOURNEY-SPEC.md` (journey structure), `qa/UI-REVIEW-2026-09-19.md` (the audit that
  drove the redesign), and the `kalinga-design-system` skill.
- **Content pitfalls:** `.claude/skills/kalinga-content-policy/pitfalls.md`. Heights: Konark 39 m 🔴 · Lingaraja null ·
  Mukteshvara ~10.5 m 🟡 · Parasuramesvara ~13 m 🔴 · Jagannath 65.4 m 🔴.
- **Media wiring without a schema change:** assets in media.json carry `temple_id`, `role` (`build-sequence` with
  `order` + `label`, or `build-video` with `poster`), and `interim`. Paths must start with `img/`; ids look like `V-64A`.
- **Environment:** no ffmpeg, sharp, Pillow or cwebp. Windows: don't put backticks or `$` inside double-quoted bash
  `node -e` strings — use the Write/Edit tools or a python heredoc for file text.
- **Git-ignored:** `visual-briefs/inbox/`, `originals/`, `references/*.jpg`, `dev/preview/`, `research/backups/`.
  `supporting-artifacts/` is the human's working folder — untracked on purpose, never commit it.
- Commit with the `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` trailer; update `BUILD_STATUS.md` after
  each milestone.
