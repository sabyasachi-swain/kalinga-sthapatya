# Handoff — Kalinga Sthapatya build (updated 2026-09-19 ~11:15 IST)

New session: read this, then `CLAUDE.md` → `BUILD_STATUS.md` → `EXECUTION_PLAN.md`.
Everything durable is on disk and in git; nothing depends on the previous chat. **The human has
limited token budget — work lean: prefer Sonnet for builders/integrator, one agent per job, short reports.**

## 1. Objective

Static educational site on Odisha (Kalinga) temple architecture for families/kids, on GitHub Pages
(repo https://github.com/sabyasachi-swain/kalinga-sthapatya → https://sabyasachi-swain.github.io/kalinga-sthapatya/).
Every fact cited (academic/ASI/UNESCO only), unverifiable → blank; facts only in `data/*.json`, pre-rendered by
`node scripts/build.mjs`; all visuals generated by the human with Gemini; evidence tiers 🟢/🟡/🔴 on every claim.

## 2. Standing decisions the human has DELEGATED (don't re-ask)

- **G2 publishing rule (confidence categories):** publish fact-checked claims tiered established / scholarly /
  uncertain (🔴 "Debated" with note). Do NOT publish needs-human, snippet/catalogue-only, rejected or sensitive
  claims (leave null).
- **Eras:** six, descriptive labels — Rock-cut roots · First temples · Experiment & refinement · Monumental scale ·
  Grand finale · Late Kalinga (already applied in `research/staged/history.json`); never "Formative/Transitional/Mature".
- **Images (G4):** approve clean PASS; keep JPEG (no Pillow install); failures stay placeholders.
- **G5 launch approved:** "proceed to publish" — push + GitHub Pages. (No `gh` CLI: the human enables Pages in
  repo Settings → Pages → Branch `main` / root.)

## 3. State at handoff

| Area | State |
|---|---|
| Guardrails, skills, 6 agents, hooks, validator, merge script | ✅ |
| `data/sources.json` (23), `data/media.json` (53 entries) | ✅ merged |
| Fact-checked & staged (`research/staged/`) | ✅ konark, lingaraj, mukteshwar, parasuramesvara, history (timeline) + `elements-names.json` (names only, no definitions) |
| Jagannath fact-check | ⏳ was running → check for `research/staged/jagannath-puri.json` (validate with `--staged`). If missing, run a fresh `kalinga-fact-checker` on `research/ledgers/jagannath-puri.json` (brief pattern in delegation-templates.md; height: Ganguly 1912 survey 214.67 ft vs Fergusson 192 ft vs Brown ~200 ft; 023 "Buddhist links" → needs-human/not published) |
| Architecture | 🟡 ledger saved, **59 claims, NOT fact-checked** (`research/ledgers/architecture.json`) — researcher was stopped to save tokens |
| Academy (5 why-questions, 6 builder steps) | ⬜ not researched (stopped before writing) |
| **NOTHING merged yet** into temples/timeline/elements/glossary/academy | ⬜ step 1 below |
| Phase 3A pages (temple template, timeline, glossary, compare) | ⏳ builder was running — `scripts/build/pages/{temple,timeline,glossary,compare}.mjs` exist; check it finished (`node scripts/build.mjs --data dev/fixtures --out dev/preview` + `--check`) |
| Phase 3B (map, academy page, home sections) | ⬜ `map.html` / `academy.html` are "coming soon" stubs |
| Images | ⏳ integrator was finalising batch-1 approved ids (V-01,03,10,11,13–15,20–24,30,31,34,36,38,40,41,50–52 → `img/`, status approved) + checking V-60–63 (Parasuramesvara) and the Jagannath folder `visual-briefs/inbox/puri-jagannath-temple/` (pick V-75 hero / V-78). Verify with `node scripts/validate-content.mjs` and `git status img/` |
| Git | `main` ahead of origin (unpushed); last commits = fact-checks + pitfalls fixes |

## 4. Next actions (in order, lean)

1. **Verify what the 3 running agents left** (row "⏳" above). Re-run only what's missing.
2. **Merge** (one command, so cross-refs resolve):
   `node scripts/merge-staged.mjs --dry-run research/staged/elements-names.json research/staged/history.json research/staged/konark.json research/staged/lingaraj.json research/staged/mukteshwar.json research/staged/parasuramesvara.json research/staged/jagannath-puri.json`
   then without `--dry-run`. Known fix-ups if validation fails: elements referenced by temples must exist
   (add names to `elements-names.json`); grand-finale era `dynasty` is null (fine). Then `node scripts/build.mjs`.
3. **Look once** (headless Chrome screenshot of `temples/konark.html` + `timeline.html`, see §6), fix only blockers.
4. **Commit, push** (`git push origin main`) → tell the human to enable Pages (Settings → Pages → main / root).
5. Then, as budget allows: fact-check `architecture` (→ elements, glossary, academy `temple_types` fragments —
   they replace `elements-names.json` entries by id); research + fact-check `academy`; Phase 3B builder
   (map with Leaflet + OSM, Odisha outline from OSM data not AI; academy page with hotspots on V-33); QA
   (`kalinga-qa-auditor`), `node scripts/validate-content.mjs --strict`.

## 5. Waiting on the human

- Regenerate images with the fix prompts in `visual-briefs/BATCH-1-REVIEW.md`: **V-33** (text + trident),
  **V-42–V-45** (domes — attach V-00 as shape reference), V-32, V-35, V-37. Prompts for all assets:
  `visual-briefs/GEMINI_PROMPTS.md`.
- Temple images still needed: Mukteshvara V-65–68, Lingaraja V-70–73, Konark V-80–84 (Jagannath arrived, unnamed).
- The 10 Jagannath "exploded" part images aren't in any brief — decide whether to add them as extra media
  (new ids like `V-75-VIMANA`) for the temple's "Parts" section.
- Reference photos (`visual-briefs/references/*_Ref_*.jpg`): source/licence unknown → never publish.

## 6. Key knowledge (don't re-learn)

- Research report = leads only, with errors; corrections in `.claude/skills/kalinga-content-policy/pitfalls.md`
  (Yayati II for Lingaraja; Mukteshvara Brown c. 975; Parasuramesvara Panigrahi c. 650 vs Brown late 8th c.;
  Jagannath 1112 start + "completed under Anangabhima III" unsupported; EI XIII article by R. D. Banerji).
- Heights: Konark 39 m 🔴 · Lingaraja null · Mukteshvara ~10.5 m 🟡 · Parasuramesvara ~13 m (Brown) 🔴 · Jagannath per its fact-check.
- archive.org **search-inside** (item `fulltext/inside.php` OCR search) = full-text evidence; locators "scan leaf N (printed page unverified)".
- Usage limits stopped agents 3× (~5 h apart): check disk, then resume/re-run.
- Preview: `python -m http.server 8080 --bind 127.0.0.1` → http://127.0.0.1:8080. Screenshots: headless Chrome
  `C:/Program Files/Google/Chrome/Application/chrome.exe --headless=new --screenshot=… --window-size=1440,2400 <url>`;
  min width ~504 px (use a 375 px iframe wrapper for mobile).
- Windows bash mangles `\\` in inline JSON — build paths with `path.win32.join` in node.
- Git-ignored: `visual-briefs/inbox/`, `originals/`, `references/*.jpg`, `dev/preview/`, `research/backups/`.
  Commit with the Co-Authored-By trailer; update `BUILD_STATUS.md` after each step.
