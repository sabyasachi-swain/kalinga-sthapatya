# Handoff — Kalinga Sthapatya (updated 2026-09-19 ~evening IST)

New session: read this, then `CLAUDE.md` → `BUILD_STATUS.md`. Everything durable is on disk and in git.
**The human has a limited token budget — work lean: Sonnet for builders/integrator, Opus for research/fact-check,
one agent per job, short reports; the human asked that big design questions go to an expert agent.**

## 1. Objective

Static educational site on Odisha (Kalinga) temple architecture for families/kids, GitHub Pages
(repo https://github.com/sabyasachi-swain/kalinga-sthapatya → https://sabyasachi-swain.github.io/kalinga-sthapatya/).
Every fact cited (academic/ASI/UNESCO only), unverifiable → blank; facts only in `data/*.json`, pre-rendered by
`node scripts/build.mjs`; visuals generated by the human (Gemini/DALL·E); tiers 🟢/🟡/🔴 on every claim.

## 2. Standing decisions (don't re-ask)

- **G2 publishing rule:** publish fact-checked established / scholarly / uncertain claims. Never publish needs-human,
  snippet/catalogue-only, rejected or sensitive claims (leave null).
- **Story voice (human, 2026-09-19, also in auto-memory):** claim `text` = kid-friendly story (ages ~8–12), vivid,
  simple; NO scholar names or debate in main text (names/debate only in the collapsed "For curious grown-ups" note;
  citations render as [1]). Safe common ground in text (e.g. "more than 1,100 years old").
- **Tiers stay but quiet:** small coloured dot + [n]; full pills only in the Sources legend.
- **Annotated images allowed for Parasuramesvara** (human override of "no text in images", 2026-09-19) — every label
  must come word-for-word from fact-checked data.
- **Eras:** six descriptive labels (Rock-cut roots · First temples · Experiment & refinement · Monumental scale ·
  Grand finale · Late Kalinga); internal ids formative/transitional/mature are keys only, never shown.
- **Images:** approve clean PASS; keep JPEG; failures stay placeholders.
- **Push:** the human approves pushes explicitly each time (they did for 71a8bd3 and 97b3cc9).

## 3. State now

| Area | State |
|---|---|
| Data merged | ✅ 5 temples (konark, lingaraj, mukteshwar, parasuramesvara, jagannath-puri), 6 eras, 11 element names (no definitions), 25 sources. Validator PASS |
| Parasuramesvara text | ✅ rewritten in story voice (`research/verdicts/parasuramesvara-story.md` = old→new table). Other 4 temples still old research-style wording |
| Pages | ✅ 13 built. UI overhaul from `qa/UI-REVIEW-2026-09-19.md` done (collapsed notes, quiet tier dots, chapter headings, accordions, compare to scale, home cards, "Soon" nav tags, mobile fixes). "See how it's built" switcher + video on Parasuramesvara |
| Images | 35 approved + interim V-64A/B/C (frames with baked unsourced labels) + V-64V video (10 s, 9.5 MB, `preload=none`), all `interim: true` in media.json. Rejected: V-71, V-72, V-73, V-76, V-77, V-81, V-82 (see `visual-briefs/BATCH-3-REVIEW.md`) |
| Git | origin/main = 97b3cc9 (live). Local, NOT pushed: handoff, `qa/TEMPLE-JOURNEY-SPEC.md`, draft annotated briefs, leads file, `parasuramesvara-2` ledger — push only with the human's OK |
| GitHub Pages | human was told to enable Settings → Pages → main / root — confirm it's live |

## 4. In flight when this session ended

1. ✅ **Researcher done** → `research/ledgers/parasuramesvara-2.json` (35 claims: 2 established, 28 scholarly, 5 uncertain;
   new source `ignca-orkhurda145` = IGNCA monument form, government doc, supporting-only). Leads doc = `research/leads/parasuramesvara-docx-leads.txt` (never cite).
   Findings that matter for labels/story: pabhaga has 3 mouldings but names disputed (Ganguly: Pada, Kumuda, Basanta —
   NOT the leads' khura/kumbha/pata); no source for a "pancharatha gandi"; Panigrahi's earth ramps are a GENERAL Bhubaneswar
   method, never applied to this temple by him (story may say "builders in Bhubaneswar…"); Brown calls the hall–tower junction
   "haphazard"; hall = two-level roof + clerestory (blocked after repairs), 6 pillars (monolithic before restoration),
   dancer/musician grilles, Brown's Della Robbia remark; Seven Mothers on hall north wall, without babies (no source says
   "earliest"); Lakulisa on front face; only Ganesha + Kartikeya niche images survive; faces west; sandstone + laterite
   foundation/compound wall; living Shiva linga; PWD repairs (Arnott report 1898–1903, rebuilt top, new kalasa, lions added)
   via Ganguly 1912; height IGNCA 12.80 m (~42 ft) vs Brown 44 ft (leads' 40.25 ft is NOT in IGNCA); leads' compound-wall
   size came from a different temple (Talesvara). Gaps: vajra-mastaka definition, window positions, plain-block doorway.
2. **Next after the ledger:** `kalinga-fact-checker` (Opus) → `research/verdicts/parasuramesvara-2.json` + a staged
   fragment that ADDS the approved claims to the parasuramesvara temple (story voice, no names in text) and element
   definitions (elements.json, replacing name-only entries by id). Validate `--staged`, then
   `node scripts/merge-staged.mjs research/staged/<file>.json`, rebuild.
3. **Then update the image prompts** in `visual-briefs/PARASURAMESVARA-ANNOTATED-BRIEFS.md` (V-64A–F): add the newly
   verified labels (part names, hall pillars/windows, Seven Mothers, sand ramps in the V-64C construction diagram).
   Only fact-checked wording. Tell the human when prompts are final — they are waiting to generate images.
4. **Then build the journey page** per `qa/TEMPLE-JOURNEY-SPEC.md` (one Sonnet `kalinga-frontend-builder` run;
   re-plan the parts-tour wiring with the new claims first). The human asked to fix the Parasuramesvara page FIRST
   and leave other pages. Verify with headless screenshots 1440 + 375 and the overlap/overflow probe; ask before push.

## 5. Waiting on the human

- Generate V-64A–F annotated images from the (to-be-updated) prompts → drop in `supporting-artifacts/parsurameswar-temple/`.
- Regenerate rejected temple images (V-71–73, V-76, V-77, V-81, V-82) and older ones (V-32, V-33, V-35, V-37, V-42–45, V-83, V-84).
- Jagannath claims 022/023 (Buddhist/Jain/"primitive cult" origins) — sensitive, needs-human, not published.
- Jagannath `temple_type` "rekha" was set only because the validator requires it (Ganguly: pancharatha deul) — review.
- Internal fact-checker remarks still in notes of the other 4 temples (e.g. "Never use for size comparisons.") —
  hidden in collapsed notes; clean up in the story-voice rewrite of those temples.

## 6. Key knowledge (don't re-learn)

- Coding task prompts for outside models (Objective/Steps/Guardrails/Output per task, shared Block C): `CODING-PROMPTS.md`. Review every result before committing.
- Research prompts for ALL topics (guardrails + per-topic prompts, usable in outside tools or as researcher briefs): `research/RESEARCH-PROMPTS.md`. Outside-tool output = leads only → `research/leads/`.
- Pitfalls/corrections: `.claude/skills/kalinga-content-policy/pitfalls.md`. Heights: Konark 39 m 🔴 · Lingaraja null ·
  Mukteshvara ~10.5 m 🟡 · Parasuramesvara ~13 m 🔴 · Jagannath 65.4 m 🔴 (Ganguly 1912 survey).
- Media wiring without schema change: media.json assets carry `temple_id`, `role` (`build-sequence` + `order` +
  `label`, `build-video` + `poster`), `interim`. Media paths must be under `img/`; ids like `V-64A`.
- Preview: `python -m http.server 8080 --bind 127.0.0.1` from repo root → http://127.0.0.1:8080. Screenshots:
  `"C:/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --hide-scrollbars --screenshot=… --window-size=1440,H <url>`;
  mobile = same-origin iframe wrapper (375px) in `dev/preview/` (git-ignored), delete after. DOM probes:
  `--dump-dom --virtual-time-budget=4000` with a script writing results into a `<pre>`.
- Windows: don't put backticks/`$` inside double-quoted bash `node -e` strings (it executed text as commands once —
  harmless, but use the Edit/Write tools for file text). No ffmpeg, Pillow, sharp, cwebp installed.
- Git-ignored: `visual-briefs/inbox/`, `originals/`, `references/*.jpg`, `dev/preview/`, `research/backups/`.
  `supporting-artifacts/` is untracked on purpose (human's working files) — don't commit it.
- Commit with the Co-Authored-By trailer; update `BUILD_STATUS.md` after each step.
